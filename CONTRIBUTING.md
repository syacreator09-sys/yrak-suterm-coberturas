# Contribución

Los cambios deben respetar `AGENTS.md`, mantener separación entre dominio e infraestructura y agregar pruebas cuando modifiquen reglas de negocio.

Convención de commits: `feat:`, `fix:`, `refactor:`, `docs:`, `test:`, `chore:`.
