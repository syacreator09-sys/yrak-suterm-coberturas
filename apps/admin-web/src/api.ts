export const API=(import.meta.env.VITE_API_BASE_URL as string|undefined)??'';const ENV_DEV_EMAIL=import.meta.env.VITE_DEV_USER_EMAIL as string|undefined;const ENV_DEV_TOKEN=import.meta.env.VITE_DEV_AUTH_TOKEN as string|undefined;const DEV_EMAIL=ENV_DEV_EMAIL??localStorage.getItem('yrakDevEmail')??undefined;const DEV_TOKEN=ENV_DEV_TOKEN??localStorage.getItem('yrakDevToken')??undefined;const GUEST_TOKEN=localStorage.getItem('yrakGuestToken')??undefined;export const hasDevSession=Boolean(DEV_EMAIL&&DEV_TOKEN);export const isGuest=Boolean(GUEST_TOKEN);export const hasSession=hasDevSession||isGuest;export function saveDevSession(email:string,token:string){localStorage.setItem('yrakDevEmail',email);localStorage.setItem('yrakDevToken',token);location.reload();}export function saveGuestSession(token:string){localStorage.setItem('yrakGuestToken',token);location.replace(location.pathname);}export function clearGuestSession(){localStorage.removeItem('yrakGuestToken');location.replace(location.pathname);}export async function api<T=any>(path:string,init:RequestInit={}){const headers=new Headers(init.headers);if(init.body&&!headers.has('content-type')&&!(init.body instanceof FormData))headers.set('content-type','application/json');if(GUEST_TOKEN){headers.set('x-yrak-guest-token',GUEST_TOKEN);}else{if(DEV_EMAIL)headers.set('x-yrak-user-email',DEV_EMAIL);if(DEV_TOKEN)headers.set('x-yrak-dev-token',DEV_TOKEN);}const r=await fetch(`${API}${path}`,{...init,headers});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.error??`HTTP ${r.status}`);return data as T;}export async function apiFile(path:string,form:FormData){return api(path,{method:'POST',body:form});}
