export function table(items:any[],keys:string[]){if(!items.length)return'<p class="muted">Sin registros.</p>';return`<div class="table-wrap"><table><thead><tr>${keys.map(k=>`<th>${k}</th>`).join('')}</tr></thead><tbody>${items.map(x=>`<tr>${keys.map(k=>`<td>${format(x[k])}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;}function format(v:any){if(v===null||v===undefined)return'';if(typeof v==='object')return`<code>${escapeHtml(JSON.stringify(v))}</code>`;return escapeHtml(String(v));}export function escapeHtml(s:string){return s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));}export function options(items:any[],value='id',label='name'){return items.map(x=>`<option value="${escapeHtml(String(x[value]))}">${escapeHtml(String(x[label]??x[value]))}</option>`).join('');}export function jsonPanel(value:any){return`<pre class="json">${escapeHtml(JSON.stringify(value,null,2))}</pre>`;}
