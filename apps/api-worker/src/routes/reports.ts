import { Hono } from 'hono';import type { AppBindings } from '../env.js';import { requireRoles } from '../middleware.js';
export const reportRoutes=new Hono<AppBindings>();reportRoutes.use('*',requireRoles('ADMIN','HR','SUPERVISOR','AUDITOR','COMMITTEE'));
function csv(rows:Record<string,unknown>[]):string{if(!rows.length)return'';const keys=Object.keys(rows[0]!);const escape=(v:unknown)=>{const s=v===null||v===undefined?'':typeof v==='object'?JSON.stringify(v):String(v);return/[",\n]/.test(s)?`"${s.replaceAll('"','""')}"`:s;};return[keys.join(','),...rows.map(r=>keys.map(k=>escape(r[k])).join(','))].join('\n');}
function response(rows:Record<string,unknown>[],filename:string){return new Response(csv(rows),{headers:{'content-type':'text/csv; charset=utf-8','content-disposition':`attachment; filename="${filename}"`}});}
reportRoutes.get('/employees.csv',async c=>{const u=c.get('user'),r=await c.env.DB.prepare(`SELECT employee_number,name,email,group_id,base_level_id,seniority_date,active FROM employees WHERE organization_id=? ORDER BY employee_number`).bind(u.organizationId).all<Record<string,unknown>>();return response(r.results??[],'employees.csv');});
reportRoutes.get('/coverage-cases.csv',async c=>{const u=c.get('user'),r=await c.env.DB.prepare(`SELECT id,group_id,target_level_id,starts_on,ends_on,effective_days,day_counting_mode,process_type,status,created_at,updated_at FROM coverage_cases WHERE organization_id=? ORDER BY created_at`).bind(u.organizationId).all<Record<string,unknown>>();return response(r.results??[],'coverage-cases.csv');});
reportRoutes.get('/audit.csv',async c=>{const u=c.get('user'),r=await c.env.DB.prepare(`SELECT id,actor_id,actor_role,entity_type,entity_id,action,rule_applied,reason,correlation_id,created_at FROM audit_events WHERE organization_id=? ORDER BY created_at,id`).bind(u.organizationId).all<Record<string,unknown>>();return response(r.results??[],'audit.csv');});
