import { Hono } from 'hono';import { cors } from 'hono/cors';import type { AppBindings,AppEnv } from './env.js';import { authenticate,correlation } from './middleware.js';import { bootstrapRoutes } from './routes/bootstrap.js';import { configurationRoutes } from './routes/configuration.js';import { employeeRoutes } from './routes/employees.js';import { coverageRoutes } from './routes/coverages.js';import { competitionRoutes } from './routes/competitions.js';import { auditRoutes } from './routes/audit.js';import { policyRoutes } from './routes/policies.js';import { meRoutes } from './routes/me.js';import { appealRoutes } from './routes/appeals.js';import { attachmentRoutes } from './routes/attachments.js';import { intakeRoutes } from './routes/intake.js';import { importRoutes } from './routes/imports.js';import { reportRoutes } from './routes/reports.js';import { offerRoutes } from './routes/offers.js';import { processNotification } from './notification-consumer.js';import { processInboundEmail } from './services/inbound-email-service.js';import { findExpiredRotationOffers } from './services/rotation-offer-service.js';import { rejectRotationCandidate } from './services/rotation-response-service.js';import { selectRotationAssignment } from './services/rotation-service.js';
const ALLOWED_ORIGIN_SUFFIXES=['.yrak-admin-web.pages.dev','.yrak-employee-portal.pages.dev'];const ALLOWED_ORIGINS=new Set(['https://yrak-admin-web.pages.dev','https://yrak-employee-portal.pages.dev','http://localhost:5173','http://localhost:5174','http://localhost:5175','http://localhost:5176']);function isAllowedOrigin(origin:string):boolean{if(ALLOWED_ORIGINS.has(origin))return true;return ALLOWED_ORIGIN_SUFFIXES.some(suffix=>origin.endsWith(suffix)&&origin.startsWith('https://'));}
const app=new Hono<AppBindings>();app.use('*',correlation);app.use('*',cors({origin:(origin)=>origin&&isAllowedOrigin(origin)?origin:undefined,allowHeaders:['content-type','x-yrak-user-email','x-correlation-id','idempotency-key'],allowMethods:['GET','POST','PUT','PATCH','DELETE','OPTIONS']}));app.get('/health',c=>c.json({ok:true,service:'yrak-suterm-coberturas-api'}));app.get('/ready',c=>c.json({ok:true,databaseConfigured:Boolean(c.env.DB),coordinatorConfigured:Boolean(c.env.GROUP_COORDINATOR)}));app.route('/bootstrap',bootstrapRoutes);app.route('/offers',offerRoutes);app.use('/v1/*',authenticate);app.route('/v1/config',configurationRoutes);app.route('/v1/policies',policyRoutes);app.route('/v1/employees',employeeRoutes);app.route('/v1/me',meRoutes);app.route('/v1/coverage-cases',coverageRoutes);app.route('/v1/competitions',competitionRoutes);app.route('/v1/appeals',appealRoutes);app.route('/v1/attachments',attachmentRoutes);app.route('/v1/intake',intakeRoutes);app.route('/v1/import',importRoutes);app.route('/v1/reports',reportRoutes);app.route('/v1/audit',auditRoutes);app.onError((error,c)=>{console.error(error);const message=error instanceof Error?error.message:'INTERNAL_ERROR';return c.json({error:message},message.endsWith('_NOT_FOUND')?404:message.endsWith('_FORBIDDEN')?403:400);});
const handler:ExportedHandler<AppEnv,{notificationId:string}>={fetch:(request,env,ctx)=>app.fetch(request,env,ctx),async queue(batch,env){for(const message of batch.messages){try{await processNotification(env,message.body.notificationId);message.ack();}catch{message.retry();}}},async scheduled(_event,env){await env.DB.prepare(`UPDATE employee_requirements SET status='EXPIRED',version=version+1,updated_at=datetime('now') WHERE status='COMPLIANT' AND valid_until IS NOT NULL AND valid_until<date('now')`).run();await env.DB.prepare(`UPDATE rotation_queue_entries SET status='AVAILABLE',version=version+1 WHERE status='RESERVED' AND NOT EXISTS(SELECT 1 FROM temporary_assignments a JOIN rotation_pools p ON p.id=rotation_queue_entries.pool_id JOIN coverage_cases c ON c.id=a.coverage_case_id WHERE a.employee_id=rotation_queue_entries.employee_id AND c.group_id=p.group_id AND a.base_level_id=p.source_level_id AND a.target_level_id=p.target_level_id AND a.status IN('PROPOSED','APPROVED','SCHEDULED','ACTIVE'))`).run();const expiredOffers=await findExpiredRotationOffers(env);for(const offer of expiredOffers){const row=await env.DB.prepare(`SELECT * FROM coverage_cases WHERE id=?`).bind(offer.coverageCaseId).first<any>();if(!row||row.status!=='CANDIDATES_CALCULATED')continue;const system={id:'SYSTEM',organizationId:offer.organizationId,email:'system@yrak.local',role:'ADMIN'} as const;try{await rejectRotationCandidate(env,system,row,`offer-expiry:${offer.assignmentId}`,{reason:'Oferta vencida - sin respuesta a tiempo'});const refreshed=await env.DB.prepare(`SELECT * FROM coverage_cases WHERE id=?`).bind(offer.coverageCaseId).first<any>();if(refreshed)await selectRotationAssignment(env,system,refreshed,`offer-expiry:${offer.assignmentId}`);}catch(error){console.error('ROTATION_OFFER_EXPIRY_SWEEP_FAILED',offer.assignmentId,error);}}const failed=await env.DB.prepare(`SELECT id FROM notifications WHERE status='FAILED' AND attempts<5 ORDER BY created_at LIMIT 100`).all<{id:string}>();for(const row of failed.results??[])await env.NOTIFICATIONS_QUEUE?.send({notificationId:row.id});},async email(message,env){await processInboundEmail(message,env);}};export { GroupCoordinator } from './group-coordinator.js';export { CoverageWorkflow } from './workflow.js';export default handler;
