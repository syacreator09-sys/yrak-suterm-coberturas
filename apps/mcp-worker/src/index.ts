import { createMcpHandler,McpServer } from '@modelcontextprotocol/server';import * as z from 'zod/v4';
interface Env{DB:D1Database;MCP_API_TOKEN:string;MCP_ORGANIZATION_ID:string}
async function log(env:Env,tool:string,args:unknown){await env.DB.prepare(`INSERT INTO mcp_access_log(id,organization_id,tool_name,arguments_json) VALUES(?,?,?,?)`).bind(crypto.randomUUID(),env.MCP_ORGANIZATION_ID,tool,JSON.stringify(args)).run();}
function createServer(env:Env){const server=new McpServer({name:'yrak-suterm-coberturas',version:'0.1.0'},{capabilities:{tools:{}}});server.registerTool('get_coverage_case',{description:'Consulta un expediente. Solo lectura.',inputSchema:z.object({caseId:z.string()})},async({caseId})=>{await log(env,'get_coverage_case',{caseId});const row=await env.DB.prepare(`SELECT * FROM coverage_cases WHERE id=? AND organization_id=?`).bind(caseId,env.MCP_ORGANIZATION_ID).first();return{content:[{type:'text',text:JSON.stringify(row??{error:'NOT_FOUND'})}]};});server.registerTool('list_rotation_queue',{description:'Consulta fila de rotación. Solo lectura.',inputSchema:z.object({groupId:z.string(),sourceLevelId:z.string(),targetLevelId:z.string()})},async(args)=>{await log(env,'list_rotation_queue',args);const rows=await env.DB.prepare(`SELECT e.employee_number,e.name,q.queue_position,q.status FROM rotation_pools p JOIN rotation_queue_entries q ON q.pool_id=p.id JOIN employees e ON e.id=q.employee_id WHERE p.organization_id=? AND p.group_id=? AND p.source_level_id=? AND p.target_level_id=? ORDER BY q.queue_position`).bind(env.MCP_ORGANIZATION_ID,args.groupId,args.sourceLevelId,args.targetLevelId).all();return{content:[{type:'text',text:JSON.stringify(rows.results??[])}]};});server.registerTool('get_employee_requirements',{description:'Consulta requisitos de trabajador. Solo lectura.',inputSchema:z.object({employeeId:z.string()})},async({employeeId})=>{await log(env,'get_employee_requirements',{employeeId});const rows=await env.DB.prepare(`SELECT r.name,r.requirement_type,er.status,er.valid_until,er.score FROM employee_requirements er JOIN requirements r ON r.id=er.requirement_id JOIN employees e ON e.id=er.employee_id WHERE er.employee_id=? AND e.organization_id=? ORDER BY r.name`).bind(employeeId,env.MCP_ORGANIZATION_ID).all();return{content:[{type:'text',text:JSON.stringify(rows.results??[])}]};});server.registerTool('get_audit_events',{description:'Consulta auditoría inmutable. Solo lectura.',inputSchema:z.object({entityType:z.string(),entityId:z.string()})},async(args)=>{await log(env,'get_audit_events',args);const rows=await env.DB.prepare(`SELECT actor_id,actor_role,action,previous_value_json,new_value_json,rule_applied,reason,created_at FROM audit_events WHERE organization_id=? AND entity_type=? AND entity_id=? ORDER BY created_at,id`).bind(env.MCP_ORGANIZATION_ID,args.entityType,args.entityId).all();return{content:[{type:'text',text:JSON.stringify(rows.results??[])}]};});return server;}
export default{async fetch(request:Request,env:Env):Promise<Response>{const auth=request.headers.get('authorization');if(!env.MCP_API_TOKEN||auth!==`Bearer ${env.MCP_API_TOKEN}`)return new Response('Unauthorized',{status:401});if(!env.MCP_ORGANIZATION_ID||env.MCP_ORGANIZATION_ID.startsWith('REPLACE_'))return new Response('MCP organization not configured',{status:503});return createMcpHandler(()=>createServer(env))(request);}} satisfies ExportedHandler<Env>;
