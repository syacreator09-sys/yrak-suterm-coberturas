const API=(import.meta.env.VITE_API_BASE_URL as string|undefined)??'';const DEV_EMAIL=import.meta.env.VITE_DEV_USER_EMAIL as string|undefined;const app=document.querySelector('#app')!;app.innerHTML='<p>Cargando expediente…</p>';const headers:Record<string,string>={};if(DEV_EMAIL)headers['x-yrak-user-email']=DEV_EMAIL;fetch(`${API}/v1/me`,{headers}).then(async r=>{if(!r.ok)throw new Error(`HTTP ${r.status}`);return r.json();}).then(data=>{app.innerHTML=`<style>body{font-family:system-ui;background:#f4f6f8;color:#172033;margin:0}main{max-width:980px;margin:auto;padding:28px}.card{background:#fff;padding:20px;border:1px solid #e2e8f0;border-radius:12px;margin-bottom:16px}table{width:100%;border-collapse:collapse}td,th{padding:9px;border-bottom:1px solid #eee;text-align:left;font-size:13px}</style><h1>Mi expediente</h1><div class="card"><h2>${data.employee?.name??data.user.email}</h2><p>Nivel base: <strong>${data.employee?.base_level_id??'—'}</strong></p></div><div class="card"><h3>Requisitos</h3><table>${(data.requirements??[]).map((x:any)=>`<tr><td>${x.name}</td><td>${x.status}</td><td>${x.valid_until??''}</td></tr>`).join('')}</table></div><div class="card"><h3>Coberturas</h3><table>${(data.assignments??[]).map((x:any)=>`<tr><td>${x.starts_on} → ${x.ends_on}</td><td>${x.base_level_id} → ${x.target_level_id}</td><td>${x.status}</td></tr>`).join('')}</table></div>`;}).catch(e=>app.innerHTML=`<p>Error: ${e}</p>`);
