# Exportaciones

- `GET /v1/reports/employees.csv`
- `GET /v1/reports/coverage-cases.csv`
- `GET /v1/reports/audit.csv`

Todos los reportes se filtran por la organización autenticada. La auditoría exportada no puede modificarse mediante la API.
